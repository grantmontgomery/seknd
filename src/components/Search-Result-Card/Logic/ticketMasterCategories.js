const ticketMasterCategories = input => {
  if (input !== undefined) {
    return `${input}`;
  } else if (input === undefined) {
    console.log(input);

    return "";
  }
};

export default ticketMasterCategories;
