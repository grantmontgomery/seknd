import React from "react";
import css from "./ScrollPosition.css";

const ScrollPosition = () => {
  return (
    <div className={`scrollPositionWrapper ${css.scrollPositionWrapper}`}></div>
  );
};

export default ScrollPosition;
